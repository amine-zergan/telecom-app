import 'package:flutter/material.dart';

class StartSite extends StatelessWidget {
  const StartSite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Ajouter un Site"),
      ),
    );
  }
}
