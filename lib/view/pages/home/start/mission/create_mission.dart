import 'package:flutter/material.dart';

class StartMission extends StatelessWidget {
  const StartMission({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Commencer une Mission"),
      ),
    );
  }
}
