import 'package:flutter/material.dart';

class StartReport extends StatelessWidget {
  const StartReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Creer Un Rapport"),
      ),
    );
  }
}
